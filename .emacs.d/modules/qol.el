(setq-default

 ;; Enable short answers for prompts
 ;; When this variable is set to t, Emacs will accept short answers (`y` or `n`)
 ;; for yes-or-no prompts, instead of requiring the full `yes` or `no`.
 use-short-answers t

 )
